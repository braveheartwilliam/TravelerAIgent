#!/bin/bash
# Simple wrapper script to start the development server

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the dev server script
"$SCRIPT_DIR/dev-server.sh"
